<nav class="navbar navbar-default main-navbar hidden-sm hidden-xs">
    <div class="container">
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

            <ul class="nav navbar-nav">
                <li class="">
                    <a href="{{path('findjob')}}">
                        <strong>Trouver un emploi</strong>
                    </a>
                </li>
                <li class="">
                    <a href="{{path('postjob')}}">
                        <strong>Créer une offre d'emploi</strong>
                    </a>
                </li>
                  <li class="">
                    <a href="{{path('contact')}}">
                        <strong>Contactez nous</strong>
                    </a>
                </li>
                   <li class="">
                    <a href="{{path('about')}}">
                        <strong>A propos de nous</strong>
                    </a>
                </li>
                
            </ul>
          {% if app.session.get('company') %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="link-profile dropdown-toggle" data-toggle="dropdown">
                            {{ app.session.get('company').company }}
                            <b class="caret"></b>
                        </a>
                    </li>
                    <li class="link-btn">
                        <a href="{{path('company_logout')}}">
                            <span class="btn btn-theme  btn-pill btn-xs btn-line">Se déconnecter</span>
                        </a>
                    </li>
                </ul>
            {% else %}
                <ul class="nav navbar-nav navbar-right">
                    <li class="link-btn">
                        <a href="{{path('login')}}">
                            <span class="btn btn-theme btn-pill btn-xs btn-line">Se connecter</span>
                        </a>
                    </li>
                    <li class="link-btn">
                        <a href="{{path('register')}}">
                            <span class="btn btn-theme  btn-pill btn-xs btn-line">Créer un compte</span>
                        </a>
                    </li>
                </ul>
            {% endif %}

        </div>
    </div>
</nav>
<!-- end main navbar -->
